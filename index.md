<div align="center">

# <span style="color:#0A2FA8">SQL Index</span>


</div>

---


## <span style="color:#1565C0">Phase 1 - Foundations & Environment Setup</span>

- 1.1 What is Data, Database, and DBMS?
- 1.2 What is RDBMS - how relational databases work
- 1.3 SQL vs NoSQL - differences, when to use what
- 1.4 What is PostgreSQL - history, why it's popular
- 1.5 Installing PostgreSQL on your OS
- 1.6 pgAdmin (GUI) setup and walkthrough
- 1.7 psql CLI - connecting, basic usage
- 1.8 psql meta-commands - `\l`, `\c`, `\d`, `\dt`, `\dn`, `\du`, `\?`, `\q`
- 1.9 What is a Schema - database vs schema vs table
- 1.10 `CREATE DATABASE`, `DROP DATABASE`
- 1.11 `CREATE SCHEMA`, `DROP SCHEMA`
- 1.12 Understanding the PostgreSQL system catalog (`pg_tables`, `information_schema`)

---

## <span style="color:#1565C0">Phase 2 - Data Types</span>

### <span style="color:#2E86AB">2.1 Numeric Types</span>
- `INTEGER`, `SMALLINT`, `BIGINT`
- `DECIMAL`, `NUMERIC` (exact precision)
- `REAL`, `DOUBLE PRECISION` (floating point)
- `SERIAL`, `BIGSERIAL` (auto-increment)

### <span style="color:#2E86AB">2.2 Text & Character Types</span>
- `CHAR(n)` - fixed length
- `VARCHAR(n)` - variable length with limit
- `TEXT` - unlimited length
- Difference between all three

### <span style="color:#2E86AB">2.3 Other Core Types</span>
- 2.3 Boolean - `TRUE`, `FALSE`, `NULL` behavior
- 2.4 Date & Time - `DATE`, `TIME`, `TIME WITH TIME ZONE`, `TIMESTAMP`, `TIMESTAMPTZ`, `INTERVAL`
- 2.5 UUID - what it is, when to use over `SERIAL`
- 2.6 Arrays - declaring, inserting, querying
- 2.7 JSON vs JSONB - differences, when to use which
- 2.8 ENUM - creating custom enum types
- 2.9 Monetary type - `MONEY`
- 2.10 Binary type - `BYTEA`
- 2.11 NULL - what NULL really means, NULL arithmetic, NULL comparisons
- 2.12 Type Casting - `::` syntax, `CAST()`

---

## <span style="color:#1565C0">Phase 3 - DDL (Data Definition Language) - Designing Tables</span>

### <span style="color:#2E86AB">3.1 Creating & Dropping Tables</span>
- `CREATE TABLE` - full syntax
- `DROP TABLE`
- `TRUNCATE TABLE` - with options (`RESTART IDENTITY`, `CASCADE`)
- DROP vs TRUNCATE vs DELETE - key differences

### <span style="color:#2E86AB">3.2 Constraints</span>
- `NOT NULL`
- `UNIQUE`
- `DEFAULT` values
- `CHECK` constraints
- `PRIMARY KEY` - single column, composite
- `FOREIGN KEY` - referencing other tables
  - `ON DELETE` CASCADE / SET NULL / SET DEFAULT / RESTRICT / NO ACTION
  - `ON UPDATE` CASCADE
- Table-level vs Column-level constraints
- Naming constraints explicitly

### <span style="color:#2E86AB">3.3 Altering Tables</span>
- `ADD COLUMN`, `DROP COLUMN`, `RENAME COLUMN`
- `ALTER COLUMN TYPE`
- `ADD CONSTRAINT`, `DROP CONSTRAINT`
- `RENAME TABLE`

### <span style="color:#2E86AB">3.4 Sequences & Identity</span>
- `CREATE SEQUENCE`
- `nextval()`, `currval()`, `setval()`
- `GENERATED ALWAYS AS IDENTITY` / `GENERATED BY DEFAULT AS IDENTITY`

### <span style="color:#2E86AB">3.5 Advanced Table Features</span>
- Temporary Tables - `CREATE TEMP TABLE`
- Table Inheritance (PostgreSQL-specific)
- Partitioning Tables
  - Range Partitioning
  - List Partitioning
  - Hash Partitioning

---

## <span style="color:#1565C0">Phase 4 - DML (Data Manipulation Language) - Working with Data</span>

### <span style="color:#2E86AB">4.1 INSERT</span>
- Single row insert
- Multi-row insert
- `INSERT` with DEFAULT values
- `INSERT INTO ... SELECT`

### <span style="color:#2E86AB">4.2 SELECT</span>
- Basic syntax, column selection, `*`

### <span style="color:#2E86AB">4.3 UPDATE</span>
- Updating single / multiple columns
- `UPDATE` with `WHERE`
- `UPDATE` with `FROM` (PostgreSQL-specific join update)

### <span style="color:#2E86AB">4.4 DELETE</span>
- `DELETE` with `WHERE`
- `DELETE` with `USING` (PostgreSQL-specific join delete)

### <span style="color:#2E86AB">4.5 PostgreSQL-Specific DML</span>
- `RETURNING` clause - with `INSERT` / `UPDATE` / `DELETE`
- UPSERT - `INSERT ... ON CONFLICT`
  - `ON CONFLICT DO NOTHING`
  - `ON CONFLICT DO UPDATE SET` (upsert)
  - Conflict target - column vs constraint name
- `COPY` command
  - `COPY FROM` (import CSV/TSV)
  - `COPY TO` (export CSV/TSV)
  - `\copy` in psql (client-side)

---

## <span style="color:#1565C0">Phase 5 - Core SELECT & Filtering</span>

### <span style="color:#2E86AB">5.1 Basic Clauses</span>
- `SELECT` clause - expressions, aliases (`AS`)
- `FROM` clause
- Table aliases

### <span style="color:#2E86AB">5.2 WHERE & Filtering</span>
- Comparison operators - `=`, `<>`, `!=`, `<`, `>`, `<=`, `>=`
- Logical operators - `AND`, `OR`, `NOT`
- Operator precedence
- `BETWEEN ... AND ...`
- `IN`, `NOT IN`
- `LIKE` - `%` and `_` wildcards
- `ILIKE` - case-insensitive LIKE (PostgreSQL-specific)
- `IS NULL` / `IS NOT NULL`

### <span style="color:#2E86AB">5.3 Sorting & Limiting</span>
- `DISTINCT` - removing duplicates
- `ORDER BY` - `ASC`, `DESC`, multi-column, `NULLS FIRST` / `NULLS LAST`
- `LIMIT` & `OFFSET` - pagination pattern
- `FETCH` - SQL standard alternative to `LIMIT`

### <span style="color:#2E86AB">5.4 Set Operations</span>
- `UNION` vs `UNION ALL`
- `INTERSECT`
- `EXCEPT`
- Rules - column count and data types must match

### <span style="color:#2E86AB">5.5 Expressions</span>
- Math expressions, string concat, `CASE` in `SELECT`

---

## <span style="color:#1565C0">Phase 6 - Aggregate Functions & Grouping</span>

### <span style="color:#2E86AB">6.1 Core Aggregate Functions</span>
- What are aggregate functions?
- `COUNT()` - `COUNT(*)` vs `COUNT(column)` vs `COUNT(DISTINCT col)`
- `SUM()`, `AVG()`, `MIN()`, `MAX()`

### <span style="color:#2E86AB">6.2 Grouping</span>
- `GROUP BY` - how it works, grouping by multiple columns
- `HAVING` - filtering after `GROUP BY`
- `WHERE` vs `HAVING` - key differences

### <span style="color:#2E86AB">6.3 Advanced Grouping</span>
- `GROUPING SETS` - multiple combinations in one query
- `ROLLUP` - subtotals and grand totals
- `CUBE` - all combinations of groupings

### <span style="color:#2E86AB">6.4 Special Aggregate Features</span>
- `FILTER` clause - `COUNT(*) FILTER (WHERE ...)`
- `DISTINCT` inside aggregates - `SUM(DISTINCT col)`
- `STRING_AGG()` - aggregate strings with separator
- `ARRAY_AGG()` - aggregate values into array
- `JSON_AGG()`, `JSONB_AGG()` - aggregate into JSON array

---

## <span style="color:#1565C0">Phase 7 - JOINs & Subqueries</span>

### <span style="color:#2E86AB">7.1 JOINs</span>
- What is a JOIN - understanding relationships
- `INNER JOIN` - only matching rows
- `LEFT JOIN` (LEFT OUTER JOIN) - all left + matching right
- `RIGHT JOIN` (RIGHT OUTER JOIN)
- `FULL OUTER JOIN` - all rows from both sides
- `CROSS JOIN` - cartesian product
- `SELF JOIN` - joining a table with itself
- Joining 3+ tables
- JOIN with aliases
- `NATURAL JOIN` (and why to avoid it)
- `LATERAL JOIN` (PostgreSQL-specific) - powerful correlated join
- JOIN vs Subquery - when to use which

### <span style="color:#2E86AB">7.2 Subqueries</span>
- Subquery in `WHERE`
- Subquery in `FROM` - derived tables / inline views
- Subquery in `SELECT` - scalar subquery
- Correlated subqueries - referencing outer query
- `EXISTS` / `NOT EXISTS`
- `ANY` / `ALL` / `SOME`
- Subquery vs JOIN - performance and readability tradeoffs

---

## <span style="color:#1565C0">Phase 8 - CTEs & Window Functions</span>

### <span style="color:#2E86AB">8.1 CTEs (Common Table Expressions)</span>
- What is a CTE - `WITH` clause syntax
- Basic CTE example
- Multiple CTEs in one query
- CTE vs Subquery vs Temp Table - when to use each
- Recursive CTEs
  - Syntax - anchor member + recursive member
  - Use cases - tree/hierarchy, sequences, graphs
  - `UNION ALL` in recursive CTEs
  - Cycle detection
- Writable CTEs - CTE with `INSERT` / `UPDATE` / `DELETE`

### <span style="color:#2E86AB">8.2 Window Functions</span>
- What are Window Functions - `OVER()` clause
- `PARTITION BY` - dividing rows into groups
- `ORDER BY` inside `OVER()`

#### <span style="color:#5B8DB8">8.2.1 Ranking Functions</span>
- `ROW_NUMBER()`
- `RANK()`
- `DENSE_RANK()`
- `NTILE(n)`
- `PERCENT_RANK()`
- `CUME_DIST()`

#### <span style="color:#5B8DB8">8.2.2 Offset Functions</span>
- `LAG(col, offset, default)`
- `LEAD(col, offset, default)`
- `FIRST_VALUE()`, `LAST_VALUE()`, `NTH_VALUE()`

#### <span style="color:#5B8DB8">8.2.3 Aggregate & Frame</span>
- Aggregate window functions - `SUM()`, `AVG()`, `COUNT()` over a window
- Running totals, moving averages
- Frame clauses - `ROWS` vs `RANGE` vs `GROUPS`
- `UNBOUNDED PRECEDING` / `FOLLOWING`, `CURRENT ROW`
- Named windows - `WINDOW` clause
- Window function vs `GROUP BY` - key differences

---

## <span style="color:#1565C0">Phase 9 - Built-in Functions</span>

### <span style="color:#2E86AB">9.1 String Functions</span>
- `CONCAT()`, `||` operator
- `LENGTH()`, `CHAR_LENGTH()`
- `UPPER()`, `LOWER()`, `INITCAP()`
- `TRIM()`, `LTRIM()`, `RTRIM()`, `BTRIM()`
- `LPAD()`, `RPAD()`
- `SUBSTRING(str FROM pos FOR len)`
- `LEFT()`, `RIGHT()`
- `POSITION()` / `STRPOS()`
- `REPLACE()`
- `SPLIT_PART()` (PostgreSQL-specific)
- `REVERSE()`, `REPEAT()`
- `FORMAT()` - printf-style formatting
- `QUOTE_IDENT()`, `QUOTE_LITERAL()`

#### <span style="color:#5B8DB8">9.1.1 Regular Expressions</span>
- `~` (match), `~*` (case-insensitive), `!~` (not match)
- `REGEXP_REPLACE()`
- `REGEXP_MATCH()`, `REGEXP_MATCHES()`
- `REGEXP_SPLIT_TO_TABLE()`, `REGEXP_SPLIT_TO_ARRAY()`

### <span style="color:#2E86AB">9.2 Numeric / Math Functions</span>
- `ROUND()`, `CEIL()`, `FLOOR()`, `TRUNC()`
- `ABS()`
- `MOD()`, `%` operator
- `POWER()`, `SQRT()`
- `RANDOM()`
- `GREATEST()`, `LEAST()`

### <span style="color:#2E86AB">9.3 Date & Time Functions</span>
- `NOW()`, `CURRENT_DATE`, `CURRENT_TIME`, `CURRENT_TIMESTAMP`
- `EXTRACT(field FROM date)` - year, month, day, hour, etc.
- `DATE_PART()` - similar to `EXTRACT`
- `DATE_TRUNC(field, timestamp)` - truncate to precision
- `AGE(timestamp, timestamp)` - difference as interval
- `INTERVAL` arithmetic - adding/subtracting time
- `TO_CHAR(date, format)` - format dates as strings
- `TO_DATE()`, `TO_TIMESTAMP()` - parse strings to date/time
- `MAKE_DATE()`, `MAKE_TIMESTAMP()`
- Timezone handling - `AT TIME ZONE`, `SHOW timezone`
- `generate_series()` with dates (PostgreSQL-specific)

### <span style="color:#2E86AB">9.4 Conditional Functions</span>
- `CASE WHEN ... THEN ... ELSE ... END` - simple and searched
- `COALESCE()` - first non-NULL value
- `NULLIF(a, b)` - returns NULL if a = b
- `GREATEST()`, `LEAST()` patterns

### <span style="color:#2E86AB">9.5 JSON / JSONB Functions (PostgreSQL-specific)</span>
- Operators - `->`, `->>`, `#>`, `#>>`
- JSONB containment - `@>`, `<@`
- Key existence - `?`, `?|`, `?&`
- `jsonb_set()`, `jsonb_insert()`
- `jsonb_each()`, `jsonb_object_keys()`
- `jsonb_agg()`, `row_to_json()`, `json_build_object()`
- `jsonb_path_query()` - JSONPath queries

### <span style="color:#2E86AB">9.6 Array Functions (PostgreSQL-specific)</span>
- Array construction - `ARRAY[]`, `ARRAY(subquery)`
- `array_append()`, `array_prepend()`, `array_cat()`
- `array_length()`, `cardinality()`
- `unnest()` - expand array to rows
- `array_agg()` - rows to array
- `ANY(array)` / `ALL(array)` in conditions
- Array operators - `@>`, `<@`, `&&`, `||`

---

## <span style="color:#1565C0">Phase 10 - Indexes & Query Performance</span>

### <span style="color:#2E86AB">10.1 Index Fundamentals</span>
- What is an Index - how it works internally (B-Tree)
- When to index - selectivity, cardinality
- When NOT to index - small tables, high-write tables
- `CREATE INDEX` syntax, `DROP INDEX`
- Unique Index
- Composite (Multi-column) Index - column order matters
- Partial Index - index with `WHERE` (PostgreSQL-specific)
- Expression / Functional Index (PostgreSQL-specific)

### <span style="color:#2E86AB">10.2 Index Types</span>

| Type | Best For |
|:---:|:---|
| B-Tree (default) | Equality, range queries |
| Hash | Equality only |
| GIN | Arrays, JSONB, full-text search |
| GiST | Geometric, full-text, range types |
| BRIN | Large sorted tables (timestamps, IDs) |

- Covering Indexes - `INCLUDE` clause
- Concurrent Index Building - `CREATE INDEX CONCURRENTLY`

### <span style="color:#2E86AB">10.3 Query Analysis</span>
- `EXPLAIN` - reading the query plan
- `EXPLAIN ANALYZE` - actual vs estimated rows
- Query plan nodes
  - Seq Scan, Index Scan, Index Only Scan, Bitmap Scan
  - Hash Join, Nested Loop, Merge Join
- Cost model basics - startup cost vs total cost

### <span style="color:#2E86AB">10.4 Maintenance & Optimization</span>
- `VACUUM` & `AUTOVACUUM` - dead tuple cleanup, statistics update
- `ANALYZE` - updating planner statistics
- `pg_stat_statements` - tracking slow queries
- Common performance anti-patterns
  - `SELECT *`
  - Functions on indexed columns in `WHERE`
  - Implicit type casts
  - N+1 query problem

---

## <span style="color:#1565C0">Phase 11 - Views, Materialized Views & Transactions</span>

### <span style="color:#2E86AB">11.1 Views</span>
- What is a View - virtual table
- `CREATE VIEW`, `DROP VIEW`, `CREATE OR REPLACE VIEW`
- Querying views
- Updatable Views - when views allow `INSERT` / `UPDATE` / `DELETE`
- `WITH CHECK OPTION`
- Security / permission views - hiding columns, rows

### <span style="color:#2E86AB">11.2 Materialized Views</span>
- What is a Materialized View - stored result set
- `CREATE MATERIALIZED VIEW`
- `REFRESH MATERIALIZED VIEW` - full refresh
- `REFRESH MATERIALIZED VIEW CONCURRENTLY`
- Indexes on Materialized Views
- View vs Materialized View - when to use each

### <span style="color:#2E86AB">11.3 Transactions</span>
- What is a Transaction
- `BEGIN` / `START TRANSACTION`
- `COMMIT`, `ROLLBACK`
- `SAVEPOINT`, `RELEASE SAVEPOINT`, `ROLLBACK TO SAVEPOINT`
- Autocommit behavior in PostgreSQL

### <span style="color:#2E86AB">11.4 ACID Properties</span>

| Property | Meaning |
|:---:|:---|
| Atomicity | All or nothing |
| Consistency | Data stays valid |
| Isolation | Concurrent transactions don't interfere |
| Durability | Committed data survives failures |

### <span style="color:#2E86AB">11.5 Isolation Levels & Concurrency</span>
- `READ UNCOMMITTED` (not really used in PG)
- `READ COMMITTED` (default in PG)
- `REPEATABLE READ`
- `SERIALIZABLE`
- Concurrency problems - Dirty Read, Non-Repeatable Read, Phantom Read

### <span style="color:#2E86AB">11.6 Locking</span>
- Table-level locks - `LOCK TABLE`
- Row-level locks - `SELECT FOR UPDATE`, `SELECT FOR SHARE`
- Advisory locks
- Deadlocks - what they are, how PostgreSQL handles them

---

## <span style="color:#1565C0">Phase 12 - Stored Procedures, Functions & Triggers</span>

### <span style="color:#2E86AB">12.1 Functions</span>
- `CREATE FUNCTION` - basic syntax
- PL/pgSQL - procedural language basics
- Function parameters - `IN`, `OUT`, `INOUT`, `VARIADIC`
- `RETURNS` - scalar, `SETOF`, `TABLE`, `VOID`
- Variable declaration - `DECLARE` block
- Control flow - `IF/ELSIF/ELSE`, `CASE`
- Loops - `LOOP`, `WHILE`, `FOR` (integer), `FOR` (query result), `FOREACH`
- Exception handling - `BEGIN / EXCEPTION / END`
- `RETURN` vs `RETURN NEXT` vs `RETURN QUERY`
- Function volatility - `IMMUTABLE`, `STABLE`, `VOLATILE`
- `SECURITY DEFINER` vs `SECURITY INVOKER`
- Default parameter values
- Function overloading
- `DROP FUNCTION`

### <span style="color:#2E86AB">12.2 Stored Procedures</span>
- `CREATE PROCEDURE` (PostgreSQL 11+)
- Difference between `FUNCTION` and `PROCEDURE`
- `CALL` statement
- Transaction control inside procedures

### <span style="color:#2E86AB">12.3 Triggers</span>
- What is a Trigger
- `CREATE TRIGGER` syntax
- `BEFORE` vs `AFTER` vs `INSTEAD OF`
- ROW-level vs STATEMENT-level triggers
- Trigger functions - `NEW`, `OLD` records
- Common use cases - audit logs, auto-updating timestamps, validation
- `DROP TRIGGER`
- Event Triggers (DDL-level triggers)

---

## <span style="color:#1565C0">Phase 13 - PostgreSQL Advanced & Unique Features</span>

### <span style="color:#2E86AB">13.1 Full-Text Search</span>
- `tsvector` - document representation
- `tsquery` - search query
- `to_tsvector()`, `to_tsquery()`, `plainto_tsquery()`, `websearch_to_tsquery()`
- `@@` match operator
- `ts_rank()`, `ts_rank_cd()` - ranking results
- GIN index for full-text search
- Dictionaries and configurations

### <span style="color:#2E86AB">13.2 Useful PostgreSQL-Specific Tools</span>
- `generate_series()` - integer, date, timestamp ranges
- `LATERAL` Joins - correlated, row-by-row subqueries
- Table Sampling - `TABLESAMPLE SYSTEM` / `BERNOULLI`
- `DISTINCT ON` - first row per group
- Range Types - `int4range`, `daterange`, `tsrange`, `numrange`

### <span style="color:#2E86AB">13.3 Custom Types</span>
- `CREATE TYPE` - composite, enum, domain
- `CREATE DOMAIN` - constrained base types

### <span style="color:#2E86AB">13.4 Extensions</span>
- What is an extension - `CREATE EXTENSION`
- `pg_stat_statements`
- `uuid-ossp` / `pgcrypto`
- `pg_trgm` - trigram similarity search
- `PostGIS` - geospatial
- `hstore` - key-value pairs

### <span style="color:#2E86AB">13.5 Other Advanced Features</span>
- Inheritance - table inheritance, use cases and caveats
- Foreign Data Wrappers (FDW) - connecting external data sources
- Notifications - `LISTEN` / `NOTIFY`
- Large Objects - `lo_create`, `lo_read`, `lo_write`
- Tablespaces - `CREATE TABLESPACE`

---

## <span style="color:#1565C0">Phase 14 - Security & User Management</span>

### <span style="color:#2E86AB">14.1 Roles & Users</span>
- Authentication vs Authorization
- `CREATE ROLE` / `CREATE USER`
- `ALTER ROLE`, `DROP ROLE`
- Role attributes - `LOGIN`, `SUPERUSER`, `CREATEDB`, `CREATEROLE`

### <span style="color:#2E86AB">14.2 Privileges</span>
- `GRANT` - on tables, schemas, databases, sequences, functions
- `REVOKE`
- Role inheritance - `GRANT role TO role`
- Default privileges - `ALTER DEFAULT PRIVILEGES`
- Schema-level permissions - `USAGE`, `CREATE`
- Column-level privileges - `GRANT SELECT (col) ON table`

### <span style="color:#2E86AB">14.3 Row-Level Security (RLS)</span>
- `ENABLE ROW LEVEL SECURITY`
- `CREATE POLICY`
- `USING` clause (read filter) vs `WITH CHECK` (write filter)
- `FORCE ROW LEVEL SECURITY` for superusers

### <span style="color:#2E86AB">14.4 Authentication & Connection Security</span>
- `pg_hba.conf` - client authentication configuration
- Authentication methods - `trust`, `md5`, `scram-sha-256`, `peer`, `ident`
- SSL / TLS connections
- Password management - `pg_authid`, password expiry
- Auditing - logging with `pg_audit` extension

---

## <span style="color:#1565C0">Phase 15 - Administration, Backup & Production</span>

### <span style="color:#2E86AB">15.1 Configuration & Tuning</span>
- PostgreSQL configuration file - `postgresql.conf`
- Key config parameters

| Parameter | Purpose |
|:---|:---|
| `max_connections` | Max concurrent clients |
| `shared_buffers` | Memory for caching data |
| `work_mem` | Memory per sort / hash operation |
| `maintenance_work_mem` | Memory for VACUUM, index builds |
| `effective_cache_size` | Planner hint for OS cache |
| `wal_level`, `max_wal_size` | Write-ahead log control |
| `log_min_duration_statement` | Slow query logging threshold |

### <span style="color:#2E86AB">15.2 Monitoring & Maintenance</span>
- System catalog tables - `pg_tables`, `pg_indexes`, `pg_stat_activity`, `pg_locks`
- Monitoring active queries - `pg_stat_activity`
- Killing long-running queries - `pg_terminate_backend()`, `pg_cancel_backend()`
- Connection pooling - concept, PgBouncer, pgpool-II
- `VACUUM` - manual vs autovacuum, bloat cleanup
- Table bloat and index bloat
- Reindexing - `REINDEX TABLE` / `REINDEX CONCURRENTLY`

### <span style="color:#2E86AB">15.3 Backup & Restore</span>
- `pg_dump` - logical backup (single database)
- `pg_dumpall` - backup all databases + roles
- `pg_restore` - restoring from `pg_dump`
- Backup formats - plain SQL, custom, directory, tar
- Continuous Archiving & PITR (Point-In-Time Recovery)
- `pg_basebackup` - physical base backup

### <span style="color:#2E86AB">15.4 Replication & High Availability</span>
- WAL (Write-Ahead Logging) - what it is
- Streaming Replication - primary/replica setup concept
- Logical Replication - replicating specific tables
- Failover and switchover concepts
- Tools overview - Patroni, PgBouncer, Repmgr

---

<div align="center">

<sub>PostgreSQL Master Index · 15 Phases · Zero to Production</sub>

</div>
